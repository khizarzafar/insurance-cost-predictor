# Core ML & Data
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2

# API Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1    # ASGI server that runs FastAPI
pydantic==2.7.1               # Data validation (used by FastAPI)

# Frontend (for Step 4)
streamlit==1.35.0
requests==2.32.3              # Streamlit uses this to call our FastAPI

# Deployment (for Step 5)
# docker is installed at the system level, not via pip
